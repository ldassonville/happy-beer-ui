apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "happy-beer-ui.fullname" . }}-test-connection"
  labels:
    {{- include "happy-beer-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "happy-beer-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
